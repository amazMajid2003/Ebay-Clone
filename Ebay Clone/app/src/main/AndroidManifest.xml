<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EBay_clone"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.myebay.SignInActivity"
            android:exported="true"
            android:label="Sign In" />
        <activity android:name=".ui.myebay.LogInActivity"
            android:exported="true"
            android:label="Log In"/>
        <activity
            android:name=".ui.myebay.SignUpActivity"
            android:exported="true"
            android:label="Sign Up"/>
        <activity
            android:name=".ui.myebay.LogUpActivity"
            android:exported="true"
            android:label="Log Up"/>

    </application>

</manifest>